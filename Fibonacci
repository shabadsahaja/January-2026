class Solution:
    def fib(n):
        dp=[-1]*(n+1)
        def fibo(n):
            if n==0:
                return 0
            if n==1:
                return 1
            if dp[n]==-1:
                dp[n]=fibo(n-1)+fibo(n-2)
            return dp[n]
        return fibo(n)

