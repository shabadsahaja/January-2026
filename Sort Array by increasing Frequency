from collections import Counter
class Solution:
    def frequencySort(self, nums):
        d = Counter(nums)
        new_lst = sorted(d.items(), key=lambda x: (x[1],-x[0]))
        result = []
        for key, val in new_lst:
            for _ in range(val):
                result.append(key)
        return result
