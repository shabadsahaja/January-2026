class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        nums.sort()
        n=len(nums)
        i=0
        while i<n-1 and nums[i]==nums[i+1]:
            i+=3
        return nums[i]
